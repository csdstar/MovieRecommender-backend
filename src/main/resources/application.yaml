spring:
  application:
    name: Backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://39.105.194.182:3306/movie_db?useSSL=false&serverTimezone=UTC
    username: movie
    password: Movie123456

server:
  port: 8080   # 修改当前后端程序运行在的端口号